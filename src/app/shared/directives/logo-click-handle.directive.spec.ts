import { LogoClickHandleDirective } from './logo-click-handle.directive';

describe('LogoClickHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoClickHandleDirective();
    expect(directive).toBeTruthy();
  });
});
